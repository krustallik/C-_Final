    using System;
    using System.Windows.Forms;
    using ListenerDatabase; // ??????? ???? ? AppDbContext

    namespace Last5Launching
    {
        internal static class Program
        {
            [STAThread]
            static void Main()
            {
                // ????????????? ???????????? ???????
                ApplicationConfiguration.Initialize();

                // ????????? ???????? ???? ?????
                using (var dbContext = new AppDbContext())
                {
                    // ????????? ????????? MyKeyboardListener ? ????????? ??????????
                    var keyboardListener = new MyKeyboardListener(dbContext);

                    // ????????? ?????, ????????? ????????? MyKeyboardListener
                    Form1 form = new Form1(keyboardListener);

                    // ?????????? ???????
                    Application.Run();
                }
            }
        }
    }
